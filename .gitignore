service/__pycache__/
__pycache__/

# Build output
app/node_modules/
app/dist/
app/dist-electron/
app/release/
app/logs/

# Third-party kernel driver source, fetched by setup (has its own upstream)
Linuwu-Sense/

# Reference checkouts of other people's drivers, kept for comparison only.
# docs/reference/README.md explains what each one settled; the code itself has
# its own upstream and is re-fetchable, so it is not tracked here.
docs/reference/*/
